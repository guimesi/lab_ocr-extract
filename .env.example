# ============================================================
# PDF processing (all optional - the app runs without a .env)
# ============================================================
# Max PDF pages processed (0 = no limit)
PDF_MAX_PAGES=0
# Reject uploads larger than this many MB
MAX_UPLOAD_MB=100
